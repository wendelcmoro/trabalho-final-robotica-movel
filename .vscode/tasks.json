{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "colcon build"
        },
        {
            "label": "install",
            "type": "shell",
            "command": ". install/setup.bash"
        },
        {
            "label": "build & install",
            "type": "shell",
            "command": "colcon build && . install/setup.bash"
        }
    ]
}